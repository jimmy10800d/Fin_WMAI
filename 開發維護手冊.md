# Fin_WMAI 薪守村 開發維護手冊

<div align="center">
  <img src="prototype/IP_ICON/IP_THINKING.png" alt="小曦雲" width="80">
  <h3>💻 開發者技術文件</h3>
  <p>本手冊提供系統架構、程式碼規範與開發指引</p>
</div>

---

## 📖 目錄

1. [專案概述](#1-專案概述)
2. [開發環境設定](#2-開發環境設定)
3. [專案結構](#3-專案結構)
4. [設計系統](#4-設計系統)
5. [前端架構](#5-前端架構)
6. [後台系統](#6-後台系統)
7. [AI 整合](#7-ai-整合)
8. [資料層](#8-資料層)
9. [程式碼規範](#9-程式碼規範)
10. [擴展指南](#10-擴展指南)
11. [測試指南](#11-測試指南)
12. [部署流程](#12-部署流程)

---

## 1. 專案概述

### 1.1 專案背景

Fin_WMAI 是基於 BDD（Behavior-Driven Development）規格設計的智慧投資理財規劃系統原型，目標是為投資新手提供以人生目標為導向的理財服務。

### 1.2 技術決策

| 項目 | 選擇 | 原因 |
|------|------|------|
| 前端框架 | Vanilla JS | 減少依賴、快速原型開發 |
| 後端服務 | Node.js + Express | RESTful API、靜態檔案服務 |
| 樣式管理 | CSS Variables | 支援主題切換 |
| AI 服務 | Ollama | 本地部署、隱私保護 |
| 資料格式 | JSON + In-Memory | 簡單易用、便於模擬 |

### 1.3 BDD 規格

專案遵循 `Fin_WMAI_BDD_V1.3_薪守村_含盟友系統_主角等級_20260211.md` 規格書，包含 A-P 功能特性：

1. Feature A：啟蒙與目標設定
2. Feature B：風險與能力評估
3. Feature C：AI 白話化投資建議
4. Feature D：交易執行與合規
5. Feature E：資產監控儀表板
6. Feature F：社交分享功能
7. Feature G：戰績回顧與任務系統
8. Feature H：里程碑與徽章系統
9. Feature I/J/K/L/M：盟友系統（邀請/權限/打氣/提醒/共同挑戰）
10. Feature N/O/P：主角等級系統（6 段位 × 5 星）

---

## 2. 開發環境設定

### 2.1 必要工具

```bash
# 開發工具
- VS Code（建議）
- Python 3.8+
- Git
- 現代瀏覽器（Chrome DevTools）

# 選用工具
- Ollama（AI 功能）
- Live Server Extension（VS Code）
```

### 2.2 VS Code 擴展推薦

```json
{
  "recommendations": [
    "ritwickdey.LiveServer",
    "esbenp.prettier-vscode",
    "ms-python.python",
    "formulahendry.auto-rename-tag",
    "christian-kohler.path-intellisense"
  ]
}
```

### 2.3 快速開始

```bash
# 1. Clone 專案
git clone https://github.com/jimmy10800d/Fin_WMAI.git
cd Fin_WMAI

# 2. 安裝依賴並啟動開發伺服器
cd prototype
npm install
node server.js

# 3. 啟動 AI 服務（可選）
ollama run llama3.1:8b

# 4. 開啟瀏覽器
# http://localhost:3000/portal.html

# 5. 執行測試（38 項 API 測試）
node tests/run-tests.js
```

---

## 3. 專案結構

### 3.1 完整目錄樹

```
Fin_WMAI/
├── prototype/                    # 前端原型 + Express 後端
│   ├── server.js                # Express 後端伺服器 (Port 3000)
│   ├── package.json             # 專案依賴 (express, cors)
│   ├── index.html               # 主應用程式入口 (SPA)
│   ├── portal.html              # 入口導航頁
│   ├── login.html               # 前台登入頁面
│   │
│   ├── css/                     # 樣式檔案
│   │   ├── styles.css           # 基礎設計系統與主題
│   │   ├── pages.css            # 各頁面特定樣式
│   │   └── login.css            # 登入頁面樣式
│   │
│   ├── js/                      # JavaScript 模組
│   │   ├── app.js               # 主應用（路由、狀態、等級、XP）
│   │   ├── chatbot.js           # AI 聊天機器人（小曦雲）
│   │   ├── data-service.js      # 資料服務層
│   │   ├── demo.js              # Demo 功能
│   │   ├── login.js             # 登入邏輯
│   │   └── pages/               # 頁面模組
│   │       ├── home.js          # 村莊廣場（首頁）
│   │       ├── goals.js         # 目標設定（Feature A）
│   │       ├── profile.js       # 風險評估（Feature B）
│   │       ├── recommendation.js # AI 建議（Feature C）
│   │       ├── execution.js     # 交易執行（Feature D）
│   │       ├── dashboard.js     # 戰績回顧（Feature E/G/H）
│   │       ├── share.js         # 冒險日誌（Feature F）
│   │       └── allies.js        # 盟友系統（Feature I-M）
│   │
│   ├── data/                    # 模擬資料
│   │   └── demo-data.json       # 完整 Demo 資料集
│   │
│   ├── IP_ICON/                 # IP 角色圖示
│   │   ├── IP_HELLO.png         # 歡迎打招呼
│   │   ├── IP_THINKING.png      # 思考載入中
│   │   ├── IP_NOTICE.png        # 注意提示
│   │   ├── IP_KEEPCARE.png      # 關懷鼓勵
│   │   ├── IP_KEEPEARN.png      # 持續獲利
│   │   ├── IP_ASSET_UP.png      # 資產上升
│   │   ├── IP_GOODNIGHT.png     # 一切安好
│   │   └── IP_NEW_CHANGE.png    # 煙然一新
│   │
│   ├── admin/                   # 後台管理系統
│   │   ├── index.html           # 後台主頁
│   │   ├── login.html           # 後台登入
│   │   ├── css/
│   │   │   └── admin.css        # 後台樣式
│   │   └── js/
│   │       ├── admin.js         # 後台邏輯
│   │       └── login.js         # 後台登入邏輯
│   │
│   ├── tests/                   # 測試檔案
│   │   └── run-tests.js         # 38 項 API 自動化測試
│   │
│   └── README.md                # Prototype 說明
│
├── DOC/                         # 歷史文件資料
├── IP_ICON/                     # 原始圖示資源
│
├── README.md                    # 專案總覽
├── 使用者手冊.md                 # 使用者操作指南
├── 系統維護手冊.md               # 系統管理指南
├── 開發維護手冊.md               # 開發者文件（本文件）
├── 薪守村_產品提案文件_V1.1*.md  # 產品提案文件
└── Fin_WMAI_BDD_V1.3*.md        # BDD 規格書（最新版）
```

### 3.2 模組依賴關係

```
                    ┌─────────────┐
                    │  index.html │
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
        ┌─────────┐  ┌─────────┐  ┌─────────┐
        │ app.js  │  │styles.css│  │pages.css│
        └────┬────┘  └─────────┘  └─────────┘
             │
    ┌────────┼────────────────────┐
    ▼        ▼                    ▼
┌────────┐ ┌──────────────┐ ┌──────────┐
│chatbot │ │ server.js    │ │pages/*.js│
│   .js  │ │ (Express)    │ │+ allies  │
└────┬───┘ └──────┬───────┘ └──────────┘
     │            │
     ▼            ▼
┌─────────┐ ┌───────────────┐
│ Ollama  │ │ RESTful API  │
│  API    │ │ In-Memory DB │
└─────────┘ └───────────────┘
```

---

## 4. 設計系統

### 4.1 CSS 變數架構

```css
/* styles.css - 設計系統核心 */

:root {
    /* ===== 莫蘭迪深色主題 ===== */
    
    /* 主色調 */
    --primary-dark: #2C363D;      /* 深度背景 */
    --primary: #3A4750;            /* 卡片背景 */
    --primary-light: #4A5860;      /* 較淺背景 */
    
    /* 強調色 - 暖棕色系 */
    --accent: #C59B85;             /* 主要強調 */
    --accent-light: #D4B5A3;       /* 淺強調 */
    --accent-dark: #A67B65;        /* 深強調 */
    
    /* 語義色彩 */
    --success: #5D9B84;            /* 成功 */
    --warning: #D4A574;            /* 警告 */
    --danger: #C27B7B;             /* 危險 */
    --info: #6A9AB0;               /* 資訊 */
    
    /* 中性色階 */
    --gray-100: #F2F5F5;
    --gray-200: #E5EAEB;
    --gray-300: #B8C9CF;
    --gray-400: #99AFB7;
    --gray-500: #7A929E;
    --gray-600: #6A828E;
    --gray-700: #4A5860;
    --gray-800: #3A4750;
    --gray-900: #2C363D;
    
    /* 主題變數 */
    --bg-primary: #2C363D;
    --bg-secondary: #3A4750;
    --text-primary: #F2F5F5;
    --text-secondary: #99AFB7;
    
    /* 特殊用途 */
    --chat-bubble-bg: #EAD7CE;     /* 小曦雲對話框 */
    --chat-bubble-text: #3A4750;
    
    /* 尺寸系統 */
    --space-xs: 4px;
    --space-sm: 8px;
    --space-md: 16px;
    --space-lg: 24px;
    --space-xl: 32px;
    
    /* 圓角 */
    --radius-sm: 8px;
    --radius-md: 12px;
    --radius-lg: 16px;
    
    /* 動畫 */
    --transition-fast: 0.15s ease;
    --transition-normal: 0.3s ease;
}
```

### 4.2 淺色主題覆蓋

```css
[data-theme="light"] {
    --bg-primary: #F2F5F5;
    --bg-secondary: #FFFFFF;
    --text-primary: #2C363D;
    --text-secondary: #4A5860;
    --text-muted: #5A7280;
    --border-color: #99AFB7;
}
```

### 4.3 主題切換實作

```javascript
// app.js - 主題切換
function toggleTheme() {
    const currentTheme = document.documentElement.getAttribute('data-theme');
    const newTheme = currentTheme === 'light' ? 'dark' : 'light';
    
    document.documentElement.setAttribute('data-theme', newTheme);
    localStorage.setItem('theme', newTheme);
    
    // 更新切換按鈕圖示
    const icon = document.querySelector('.theme-toggle-btn i');
    icon.className = newTheme === 'light' ? 'fas fa-moon' : 'fas fa-sun';
    
    // 重新渲染需要主題適配的元件（如儀表板圖表）
    if (AppState.currentPage === 'dashboard' && typeof renderAssetChart === 'function') {
        renderAssetChart();
    }
}
```

### 4.4 圖表主題適配

資產變化直方圖使用 CSS 變數實現自動主題適配：

```css
/* 圖表樣式使用 CSS 變數 */
.asset-bar-value {
    color: var(--text-primary);  /* 自動跟隨主題 */
}
.asset-bar-label {
    color: var(--text-secondary);
}
.asset-bar-dot {
    border: 3px solid var(--bg-card);  /* 背景色邊框 */
}
```

---

## 5. 前端架構

### 5.1 應用程式狀態管理

```javascript
// app.js - 全域狀態
const AppState = {
    currentPage: 'home',
    user: {
        name: '官大大',
        status: '新手投資者',
        riskScore: null,
        riskGrade: null
    },
    theme: 'dark',
    goals: [],
    currentGoal: null,
    profile: null,
    recommendation: null,
    actionList: [],
    riskDisclosureAcknowledged: false,
    events: []  // 事件追蹤
};
```

### 5.2 路由系統

```javascript
// SPA 路由機制
function navigateTo(page) {
    // 更新狀態
    AppState.currentPage = page;
    
    // 更新導航
    document.querySelectorAll('.nav-link').forEach(link => {
        link.classList.toggle('active', link.dataset.page === page);
    });
    
    // 載入頁面內容
    loadPageContent(page);
    
    // 記錄事件
    logEvent('page_view', { page });
}

function loadPageContent(page) {
    const mainContent = document.getElementById('mainContent');
    
    // 根據頁面載入對應內容
    switch(page) {
        case 'home':
            renderHomePage(mainContent);
            break;
        case 'goals':
            renderGoalsPage(mainContent);
            break;
        // ... 其他頁面
    }
}
```

### 5.3 頁面模組模式

每個頁面模組遵循相同的結構：

```javascript
// pages/goals.js - 頁面模組範例

/**
 * 渲染目標設定頁面
 * @param {HTMLElement} container - 目標容器
 */
function renderGoalsPage(container) {
    container.innerHTML = `
        <div class="page-header">
            <h1>目標設定</h1>
        </div>
        <div class="goals-container">
            ${renderGoalTags()}
        </div>
    `;
    
    // 綁定事件
    bindGoalEvents();
}

function renderGoalTags() {
    const goals = [
        { id: 'retirement', name: '退休生活', icon: '🌴' },
        { id: 'house', name: '買房頭期款', icon: '🏠' },
        // ...
    ];
    
    return goals.map(goal => `
        <div class="goal-tag" data-goal="${goal.id}">
            <span class="goal-icon">${goal.icon}</span>
            <span class="goal-name">${goal.name}</span>
        </div>
    `).join('');
}

function bindGoalEvents() {
    document.querySelectorAll('.goal-tag').forEach(tag => {
        tag.addEventListener('click', () => selectGoal(tag.dataset.goal));
    });
}
```

### 5.4 事件追蹤系統

```javascript
// 事件記錄函數
function logEvent(eventName, data = {}) {
    const event = {
        event: eventName,
        timestamp: new Date().toISOString(),
        userId: AppState.user?.id || 'anonymous',
        sessionId: sessionStorage.getItem('sessionId'),
        ...data
    };
    
    AppState.events.push(event);
    console.log('📊 Event:', event);
    
    // 未來可發送至分析服務
    // sendToAnalytics(event);
}

// 使用範例
logEvent('goal_selected', { goalId: 'retirement', goalAmount: 5000000 });
logEvent('kyc_completed', { riskScore: 65, riskGrade: 'moderate' });
```

---

## 6. 後台系統

### 6.1 後台架構

```javascript
// admin/js/admin.js - 後台主程式

const AdminState = {
    currentPage: 'dashboard',
    currentUser: null,
    isAuthenticated: false
};

// 頁面渲染器
const PageRenderers = {
    dashboard: renderDashboardPage,
    knowledge: renderKnowledgePage,
    faq: renderFaqPage,
    prompts: renderPromptsPage,
    datasource: renderDatasourcePage,
    risk: renderRiskPage,
    compliance: renderCompliancePage,
    audit: renderAuditPage,
    users: renderUsersPage,
    roles: renderRolesPage,
    settings: renderSettingsPage
};
```

### 6.2 CRUD 操作模式

```javascript
// 知識庫 CRUD 範例
const KnowledgeManager = {
    items: [],
    
    async load() {
        // 模擬 API 呼叫
        this.items = await fetchKnowledgeData();
        this.render();
    },
    
    create(item) {
        item.id = Date.now();
        item.createdAt = new Date().toISOString();
        this.items.push(item);
        this.render();
        showToast('知識條目已新增', 'success');
    },
    
    update(id, updates) {
        const index = this.items.findIndex(i => i.id === id);
        if (index !== -1) {
            this.items[index] = { ...this.items[index], ...updates };
            this.render();
            showToast('知識條目已更新', 'success');
        }
    },
    
    delete(id) {
        this.items = this.items.filter(i => i.id !== id);
        this.render();
        showToast('知識條目已刪除', 'info');
    },
    
    render() {
        const container = document.getElementById('knowledgeList');
        container.innerHTML = this.items.map(renderKnowledgeCard).join('');
    }
};
```

### 6.3 權限控制

```javascript
// 角色權限定義
const Permissions = {
    admin: ['*'],  // 所有權限
    manager: ['knowledge', 'faq', 'prompts', 'datasource'],
    auditor: ['audit', 'compliance'],  // 唯讀
    viewer: ['dashboard']
};

// 權限檢查
function hasPermission(action) {
    const role = AdminState.currentUser?.role || 'viewer';
    const permissions = Permissions[role];
    return permissions.includes('*') || permissions.includes(action);
}

// 使用範例
if (!hasPermission('knowledge')) {
    showToast('您沒有此操作的權限', 'error');
    return;
}
```

---

## 7. AI 整合

### 7.1 Ollama 配置

```javascript
// chatbot.js - AI 服務配置

const OllamaConfig = {
    baseUrl: 'http://localhost:11434',
    model: 'llama3.1:8b',  // 預設模型，會自動偵測可用模型
    enabled: true,
    timeout: 60000,
    systemPrompt: `你是「小曦雲」，一位親切、專業的智慧理財小助手。
- 使用繁體中文回答
- 說話親切友善，適時使用表情符號
- 專注於投資理財相關話題
- 回答簡潔明瞭，使用列點和分段
- 提醒用戶投資有風險`
};
```

### 7.2 模型自動偵測

當預設模型不可用時，系統會自動偵測並切換到可用的 Ollama 模型：

```javascript
// 自動偵測可用模型
async function checkOllamaStatus() {
    try {
        const response = await fetch(`${OllamaConfig.baseUrl}/api/tags`);
        const data = await response.json();
        
        if (data.models && data.models.length > 0) {
            // 檢查預設模型是否可用
            const defaultAvailable = data.models.some(
                m => m.name === OllamaConfig.model
            );
            
            if (!defaultAvailable) {
                // 自動切換到第一個可用模型
                OllamaConfig.model = data.models[0].name;
                console.log(`🔄 Auto-switched to model: ${OllamaConfig.model}`);
            }
            
            return true;
        }
    } catch (error) {
        return false;
    }
}
```

### 7.3 API 呼叫

```javascript
// 呼叫 Ollama API
async function callOllamaAPI(userMessage) {
    // 準備對話歷史
    const messages = [
        { role: 'system', content: OllamaConfig.systemPrompt },
        ...ChatbotState.conversationHistory,
        { role: 'user', content: userMessage }
    ];
    
    try {
        const response = await fetch(`${OllamaConfig.baseUrl}/api/chat`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
                model: OllamaConfig.model,
                messages: messages,
                stream: false,
                options: {
                    temperature: 0.7,
                    num_ctx: 4096
                }
            }),
            signal: AbortSignal.timeout(OllamaConfig.timeout)
        });
        
        if (!response.ok) {
            throw new Error(`Ollama API error: ${response.status}`);
        }
        
        const data = await response.json();
        return data.message.content;
        
    } catch (error) {
        console.error('Ollama API failed:', error);
        return null;  // 回退到本地知識庫
    }
}
```

### 7.3 個人資料查詢整合

```javascript
// 將用戶資料注入 AI 對話
function buildContextPrompt(userMessage) {
    let context = '';
    
    // 檢測查詢意圖
    if (containsKeywords(userMessage, ['資產', '多少錢', '帳戶'])) {
        const summary = PersonalDataQueries.getAssetSummary();
        context += `用戶資產資訊：${JSON.stringify(summary)}\n`;
    }
    
    if (containsKeywords(userMessage, ['持倉', '基金', '股票'])) {
        const holdings = PersonalDataQueries.getHoldings();
        context += `持倉明細：${JSON.stringify(holdings)}\n`;
    }
    
    return context + userMessage;
}
```

### 7.4 本地知識庫回退

```javascript
// 當 AI 不可用時的本地回答
const LocalKnowledge = {
    patterns: [
        {
            keywords: ['資產', '總覽', '多少錢'],
            handler: () => {
                const data = PersonalDataQueries.getAssetSummary();
                return formatAssetSummaryResponse(data);
            }
        },
        {
            keywords: ['定期定額', '是什麼'],
            response: '定期定額是一種投資策略，每月固定投入一定金額...'
        }
        // ... 更多規則
    ],
    
    findAnswer(message) {
        for (const pattern of this.patterns) {
            if (containsKeywords(message, pattern.keywords)) {
                return pattern.handler 
                    ? pattern.handler() 
                    : pattern.response;
            }
        }
        return null;
    }
};
```

---

## 8. 資料層

### 8.1 Demo 資料結構

```json
// data/demo-data.json 結構
{
    "customers": [
        {
            "id": "cust_001",
            "name": "官大大",
            "email": "kuan@example.com",
            "riskProfile": {
                "score": 65,
                "grade": "moderate"
            },
            "tags": ["新手投資者", "科技業"]
        }
    ],
    "accounts": [...],
    "holdings": [...],
    "transactions": [...],
    "goals": [...],
    "products": [...],
    "market": {...}
}
```

### 8.2 資料服務層

```javascript
// data-service.js - 資料服務

const demoDataService = {
    data: null,
    loaded: false,
    
    async loadData() {
        try {
            const response = await fetch('data/demo-data.json');
            this.data = await response.json();
            this.loaded = true;
            console.log('✅ Demo data loaded');
        } catch (error) {
            console.error('Failed to load demo data:', error);
        }
    },
    
    // 查詢方法
    getCustomerById(id) {
        return this.data?.customers?.find(c => c.id === id);
    },
    
    getCustomerAccounts(customerId) {
        return this.data?.accounts?.filter(a => a.customerId === customerId);
    },
    
    getCustomerHoldings(customerId) {
        return this.data?.holdings?.filter(h => h.customerId === customerId);
    },
    
    getCustomerAccountSummary(customerId) {
        const holdings = this.getCustomerHoldings(customerId);
        const totalValue = holdings.reduce((sum, h) => sum + h.marketValue, 0);
        const totalReturn = holdings.reduce((sum, h) => sum + h.unrealizedPL, 0);
        
        return {
            totalValue,
            totalReturn,
            returnRate: totalReturn / (totalValue - totalReturn)
        };
    }
};
```

---

## 9. 程式碼規範

### 9.1 命名規範

```javascript
// 變數：camelCase
const userName = 'John';
const isLoggedIn = true;

// 常數：UPPER_SNAKE_CASE
const MAX_RETRY_COUNT = 3;
const API_BASE_URL = 'http://localhost:11434';

// 函數：camelCase，動詞開頭
function getUserData() {}
function handleClick() {}
function renderPage() {}

// 類別/建構子：PascalCase
class ChatbotManager {}
const AppState = {};

// CSS 類別：kebab-case
.user-profile {}
.chat-message-bubble {}
.nav-link-active {}
```

### 9.2 檔案組織

```javascript
/**
 * 檔案頭部註解
 * filename.js - 功能說明
 * 
 * @description 詳細描述
 * @author 作者
 * @version 1.0
 */

// ===== 1. 常數定義 =====
const CONFIG = {};

// ===== 2. 狀態定義 =====
const State = {};

// ===== 3. 工具函數 =====
function helperFunction() {}

// ===== 4. 核心邏輯 =====
function mainLogic() {}

// ===== 5. 事件處理 =====
function handleEvent() {}

// ===== 6. 渲染函數 =====
function renderComponent() {}

// ===== 7. 初始化 =====
function init() {}

// ===== 8. 導出（如需要）=====
// export { init, State };
```

### 9.3 CSS 組織

```css
/* 1. 變數定義 */
:root { }

/* 2. Reset & Base */
* { box-sizing: border-box; }

/* 3. Typography */
h1, h2, h3 { }

/* 4. Layout */
.container { }
.sidebar { }

/* 5. Components */
.card { }
.button { }

/* 6. Page-specific */
.home-page { }

/* 7. Utilities */
.text-center { }
.mt-1 { }

/* 8. Media Queries */
@media (max-width: 768px) { }
```

### 9.4 註解規範

```javascript
/**
 * 函數說明
 * @param {string} userId - 使用者 ID
 * @param {Object} options - 選項
 * @returns {Promise<Object>} 使用者資料
 */
async function getUserById(userId, options = {}) {
    // 單行註解：說明重要邏輯
    
    /*
     * 多行註解：
     * 說明複雜邏輯或
     * 重要決策原因
     */
}

// TODO: 待辦事項
// FIXME: 需修復的問題
// HACK: 臨時解決方案
// NOTE: 重要說明
```

---

## 10. 擴展指南

### 10.1 新增頁面

```javascript
// 1. 建立頁面模組 pages/newpage.js
function renderNewPage(container) {
    container.innerHTML = `
        <div class="page-header">
            <h1>新頁面</h1>
        </div>
        <div class="page-content">
            <!-- 頁面內容 -->
        </div>
    `;
    bindNewPageEvents();
}

// 2. 在 app.js 註冊路由
case 'newpage':
    renderNewPage(mainContent);
    break;

// 3. 在 index.html 添加導航
<a class="nav-link" data-page="newpage">
    <i class="fas fa-plus"></i>
    <span>新頁面</span>
</a>

// 4. 在 pages.css 添加樣式
.newpage-specific-style { }
```

### 10.2 新增 AI 功能

```javascript
// 1. 在 chatbot.js 添加處理器
const NewFeatureHandler = {
    keywords: ['新功能關鍵字'],
    
    canHandle(message) {
        return containsKeywords(message, this.keywords);
    },
    
    async handle(message) {
        // 處理邏輯
        return '回應訊息';
    }
};

// 2. 註冊到處理鏈
MessageHandlers.push(NewFeatureHandler);
```

### 10.3 新增資料欄位

```json
// 1. 更新 demo-data.json
{
    "newEntity": [
        { "id": "new_001", "field": "value" }
    ]
}

// 2. 更新 data-service.js
getNewEntity(id) {
    return this.data?.newEntity?.find(e => e.id === id);
}
```

---

## 11. 測試指南

### 11.1 手動測試檢查清單

```markdown
## 功能測試清單

### 登入流程
- [ ] 輸入有效帳號可進入 QR 驗證
- [ ] QR Code 正確顯示
- [ ] 掃描成功可跳轉首頁
- [ ] 登出後無法直接進入首頁

### 目標設定
- [ ] 所有目標標籤可點擊
- [ ] 輸入金額、期程可計算
- [ ] 結果正確顯示

### AI 聊天
- [ ] 可開啟/關閉對話視窗
- [ ] 訊息正確送出/接收
- [ ] Ollama 離線時本地回答可用
- [ ] 自動偵測可用模型（當預設模型不存在時）

### 主題切換
- [ ] 深色/淺色正確切換
- [ ] 所有頁面樣式正確
- [ ] 資產變化直方圖顏色自動適配主題
- [ ] 重新整理後主題保持
```

### 11.2 瀏覽器相容性

| 瀏覽器 | 最低版本 | 測試狀態 |
|--------|---------|---------|
| Chrome | 90+ | ✅ |
| Firefox | 88+ | ✅ |
| Edge | 90+ | ✅ |
| Safari | 14+ | ⚠️ |

### 11.3 效能測試

```javascript
// 效能計時
console.time('page-load');
await loadPageContent('home');
console.timeEnd('page-load');  // 目標 < 500ms

console.time('ai-response');
await callOllamaAPI('test');
console.timeEnd('ai-response');  // 目標 < 10s
```

---

## 12. 部署流程

### 12.1 部署檢查清單

```markdown
## 部署前檢查

### 程式碼
- [ ] 所有 console.log 已移除或改為條件式
- [ ] 錯誤處理完整
- [ ] 敏感資訊未硬編碼

### 資源
- [ ] 圖片已優化
- [ ] CSS/JS 可考慮壓縮
- [ ] 快取策略設定

### 設定
- [ ] API URL 設定正確
- [ ] 預設帳號密碼已變更
- [ ] 錯誤頁面已設定
```

### 12.2 部署步驟

```bash
# 1. 建立部署目錄
mkdir /var/www/finwmai

# 2. 複製檔案並安裝依賴
cp -r prototype/* /var/www/finwmai/
cd /var/www/finwmai
npm install --production

# 3. 使用 PM2 啟動 Express 服務
npm install -g pm2
pm2 start server.js --name finwmai

# 4. 設定 Nginx 反向代理（可選）
# Nginx 範例
server {
    listen 80;
    server_name finwmai.example.com;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# 5. 設定 Ollama（如需要）
# 確保 Ollama 服務在背景運行

# 6. 驗證部署
curl http://localhost:3000/api/health
curl http://finwmai.example.com/portal.html
```

### 12.3 版本管理

```bash
# 語意化版本
# MAJOR.MINOR.PATCH
# 1.0.0 -> 1.0.1 (修復)
# 1.0.0 -> 1.1.0 (新功能)
# 1.0.0 -> 2.0.0 (破壞性變更)

# Git 標籤
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

---

## 📚 參考資源

- [BDD 規格書 V1.3](Fin_WMAI_BDD_V1.3_薪守村_含盟友系統_主角等級_20260211.md)
- [產品提案文件 V1.1](薪守村_產品提案文件_V1.1_含主角等級_20260211.md)
- [Ollama 文件](https://ollama.ai/docs)
- [MDN Web Docs](https://developer.mozilla.org/)
- [Express.js 文件](https://expressjs.com/)

---

<div align="center">
  <p>Fin_WMAI 薪守村 開發維護手冊 v2.0</p>
  <p>© 2026 Fin_WMAI Team</p>
</div>
